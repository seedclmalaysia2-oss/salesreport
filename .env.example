# Copy this file to `.env` and fill in the values from
# Supabase Studio → Settings → API.
#
# IMPORTANT: `.env` is gitignored. NEVER commit the service_role key.

# --- Frontend (Vite, exposed to the browser) ---
VITE_SUPABASE_URL=https://fgqiwitiqwftvfhkchpt.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJI...  # the publishable / anon key

# --- Server-side scripts (DO NOT prefix with VITE_) ---
SUPABASE_URL=https://fgqiwitiqwftvfhkchpt.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJI...  # service_role key — bypasses RLS
